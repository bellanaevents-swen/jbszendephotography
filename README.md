Stack & Infrastructure SpecificationsComponentTechnical DetailSpecs / LimitsCompute InstanceRender Web Service (Free Tier)0.1 CPU core, 512 MB RAMNode.js RuntimeNode v24.14.1ES Modules ("type": "module")DatabaseMongoDB Atlas (M0 Shared Tier)512 MB Storage, 100 max connectionsMedia DeliveryCloudinary CDN25 GB monthly bandwidth, 25 Credits/moBuild SystemVite 5.x SPA + Express MiddlewareProduction static assets served from /distRuntime & Server BehaviorSpin-down Behavior: Render's free instance automatically goes to sleep after 15 minutes of inactivity. The first request after a sleep period triggers a "cold start" taking roughly 30 to 50 seconds to boot Node.js and re-establish the MongoDB connection pool. Subsequent requests respond in milliseconds.Monthly Active Hours: Render provides 750 free instance hours per month, which is enough to run a single Web Service 24/7 without exceeding free allowances.Port Binding: Binds dynamically to process.env.PORT on network interface 0.0.0.0 inside Render's container isolation layer.SSL/TLS Certificate: TLS 1.3 encryption provided automatically via Let's Encrypt with auto-renewal.Database & Network BenchmarksRead/Write Latency: ~30 ms – 70 ms query execution time from Render servers to MongoDB Atlas AWS region.Asset Payload Optimization: Images handled via Cloudinary deliver WebP/AVIF auto-format selection, keeping hero images under 200 KB on average.CORS & API Security: Restricted payload parser with express.json() and secured write endpoints using standard JWT (JSON Web Tokens) signed via your JWT_SECRET.
